version: '3.8'

# Full-stack deployment with frontend and backend
services:
  # Backend API
  api:
    build:
      context: https://dev.azure.com/diakonos-analytics/vine-and-fig-api/_git/vine-and-fig-api#master
      dockerfile: Dockerfile
    container_name: vine-and-fig-api
    ports:
      - "8080:8080"
    environment:
      # Server Settings
      - HOST=0.0.0.0
      - PORT=8080
      
      # CORS Origins - Frontend URL
      - CORS_ORIGINS=["http://localhost:3000","http://frontend:3000"]
      
      # Storage Type - "local" or "azure"
      - STORAGE_TYPE=azure
      
      # Local File Storage
      - TEMP_DIR=/app/temp
      
      # Azure Storage Settings (uncomment when using Azure)
      - AZURE_STORAGE_ACCOUNT_NAME=savineandfig
      - AZURE_STORAGE_SAS_TOKEN=sv=2024-11-04&ss=b&srt=sco&sp=rwdlac&se=2026-12-31T12:18:56Z&st=2025-10-02T03:03:56Z&spr=https&sig=WNvT6YeDz12%2B70KHrI1g2u%2FlMl2%2Fek6BWbaiICRpxA8%3D
      - AZURE_STORAGE_CONTAINER_NAME=data
      
      # File Cleanup
      - FILE_MAX_AGE=3600
      
      # API Settings
      - API_V1_PREFIX=/api
    
    volumes:
      - ./temp:/app/temp
    
    restart: unless-stopped
    
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    
    networks:
      - vine-and-fig-network
    
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 512M

  # Frontend
  frontend:
    build:
      context: https://dev.azure.com/diakonos-analytics/vine-and-fig/_git/vine-and-fig#master
      dockerfile: Dockerfile
    container_name: vine-and-fig-frontend
    ports:
      - "3000:3000"
    environment:
      # Point to the backend API service
      - VITE_API_URL=http://localhost:8080
    depends_on:
      - api
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - vine-and-fig-network

networks:
  vine-and-fig-network:
    driver: bridge

volumes:
  temp:
    driver: local
